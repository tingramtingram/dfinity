type videoIds = vec nat;
type UserUpdate = record {userData: UserData;};
type UserData = 
 record {
   dateOfBirth: opt text;
   name: opt text;
   profilePhotos: vec opt text;
 };
type User = 
 record {
   id: principal;
   userData: UserData;
 };
type ChunkId = nat;
type ChunkData = text;
service : {
  create: (UserUpdate) -> (text);
  delete: () -> (bool);
  getChunk: (ChunkId) -> (opt ChunkData);
  getVideoIds: () -> (opt videoIds);
  read: () -> (opt User);
  setChunk: (ChunkId, ChunkData) -> (text);
  setVideoIds: (videoIds) -> (text);
  update: (User) -> (bool);
  whoami: () -> (principal);
}
